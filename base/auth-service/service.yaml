apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: production-retail
  labels:
    app: auth-service
spec:
  ports:
    - name: http
      port: 8081
      targetPort: 8081
  selector:
    app: auth-service
  type: ClusterIP
