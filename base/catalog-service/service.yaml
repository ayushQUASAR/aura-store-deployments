apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: production-retail
  labels:
    app: catalog-service
spec:
  ports:
    - name: http
      port: 8082
      targetPort: 8082
  selector:
    app: catalog-service
  type: ClusterIP
