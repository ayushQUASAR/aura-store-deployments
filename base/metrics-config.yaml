apiVersion: v1
kind: ConfigMap
metadata:
  name: metrics-config
  namespace: production-retail
data:
  application.yaml: |
    management:
      metrics:
        distribution:
          percentiles-histogram:
            http:
              server:
                requests: true
          percentiles:
            http:
              server:
                requests: 0.5,0.95,0.99