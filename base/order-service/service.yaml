apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: production-retail
  labels:
    app: order-service
spec:
  ports:
    - name: http
      port: 8083
      targetPort: 8083
  selector:
    app: order-service
  type: ClusterIP
